#此配置文件只是个demo，对开发没什么卵用

#配置端口号（默认8080）
port: 8088

#Mars-config 远程配置中心
config:
  # 本服务的名称，在整个分布式集群中必须唯一
  name: user1
  # 本服务的IP【预留属性，后续版本会发挥它的作用】
  myIp: 127.0.0.1
  # Mars-config 服务的 ip:端口号，或者 host:端口号，或者 域名:端口号
  # 不可以指定协议，必须是 本示例中的各式
  url: 127.0.0.1:8090